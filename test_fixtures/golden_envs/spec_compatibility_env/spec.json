{
  "apiVersion": "tanka.dev/v1alpha1",
  "kind": "Environment",
  "metadata": {},
  "spec": {
    "apiServer": "cluster.example:6443",
    "namespace": "default",
    "diffStrategy": "native",
    "applyStrategy": "future-apply",
    "expectVersions": {
      "tanka": ">= 0.0.0 || < 0.0.0",
      "kubectl": ">= 0.0.0"
    }
  }
}
