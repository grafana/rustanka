apiVersion: batch/v1
kind: Job
metadata:
  name: example-hook
  annotations:
    helm.sh/hook: pre-install
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: hook
          image: busybox
