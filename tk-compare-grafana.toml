# Configuration for comparing Grafana Tanka (tk) with rustanka (rtk)
# This config runs against grafana/deployment_tools
#
# Usage with Makefile (recommended):
#   make tk-compare-grafana DEPLOYMENT_TOOLS_PATH=/path/to/deployment_tools TK_PATH=/path/to/tk
#
# Environment variables:
#   TK_PATH                - Path to tk executable (required)
#   DEPLOYMENT_TOOLS_PATH  - Path to deployment_tools directory (required)

# Path to Grafana's Tanka (from TK_PATH env var)
tk_exec_1 = "${TK_PATH}"
tk_exec_1_name = "tk"

# Path to rustanka's rtk
tk_exec_2 = "target/release/rtk"
tk_exec_2_name = "rtk"

# Working directory for all commands (from DEPLOYMENT_TOOLS_PATH env var)
working_dir = "${DEPLOYMENT_TOOLS_PATH}/ksonnet"

# Commands to test
# These commands test various Tanka functionality against real Grafana infrastructure configs
# Optional 'runs' parameter specifies how many times to run the command for benchmark statistics
#
# For export commands, use {{EXEC_NAME}} placeholder in output directory to create separate
# output directories for each executable, enabling directory comparison.

# ==================== TOOL IMPORTERS COMMANDS ====================
[[commands]]
name = "tool importers: env files"
args = [
    "tool", "importers", 
    "environments/cortex/prod-eu-west-0.mimir-dedicated-33/automated-horizontal-scaling.json", 
    "environments/cortex/prod-eu-west-0.mimir-dedicated-33/automated-scaling.json",
    "environments/cortex/prod-eu-west-0.mimir-dedicated-33/main.jsonnet",
    "environments/cortex/prod-eu-west-0.mimir-dedicated-33/temp.libsonnet",
]
runs = 3

[[commands]]
name = "tool importers: hosted-grafana"
args = [
    "tool", "importers", 
    "environments/hosted-grafana/dev/config/dev-cd.libsonnet",
    "environments/hosted-grafana/waves/channels/main.libsonnet",
]
runs = 1

[[commands]]
name = "tool importers: deleted env file"
args = [
    "tool", "importers", 
    "deleted:environments/cortex/prod-eu-west-0.mimir-dedicated-33/temp.libsonnet",
]
runs = 3

[[commands]]
name = "tool importers: env+lib files"
args = [
    "tool", "importers", 
    "environments/grafana-iam/iam-grafana-app-config.jsonnet", 
    "environments/grafana-iam/iam-grafana-app.jsonnet",
    "lib/argo-workflows-util/utils/cron-workflow.libsonnet",
]
runs = 3

[[commands]]
name = "tool importers: deleted lib files"
args = [
    "tool", "importers", 
    "deleted:ksonnet/lib/external-secrets/gar-migration/argo-workflows.libsonnet",
    "deleted:ksonnet/lib/grafana-unified-storage/database.libsonnet",
    "deleted:ksonnet/lib/grafana_com/regions-and-cells/cells.libsonnet",
]
runs = 3

[[commands]]
name = "tool importers: all main files"
args = [
    "tool", "importers", "{{LIST_MAIN_FILES}}",
]
runs = 1

# ==================== EXPORT COMMANDS ====================
# Export commands use {{EXEC_NAME}} placeholder for separate output directories
# {{EXPORT_FORMAT}} is the format string we use for tk
# Note: Export is slower, so fewer runs. dir_compare compares the output directories.

[[commands]]
name = "export: mimir-ops-03"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/mimir-ops-03", "environments/cortex/ops-eu-south-0.mimir-ops-03", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: grafana-apps"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}-grafana-apps", "environments/grafana-apps", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: billing-prometheus"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}-billing-prometheus", "environments/billing-prometheus", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: federal-ge-grafana"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}-federal-ge-grafana", "environments/federal-ge-grafana", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: federal-ge-metrics"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}-federal-ge-metrics", "environments/federal-ge-metrics", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: federal-ge-traces"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}-federal-ge-traces", "environments/federal-ge-traces", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: flagger"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}-flagger", "environments/flagger", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: o11y-apps-platform"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}-o11y-apps-platform", "environments/o11y-apps-platform", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true


[[commands]]
name = "export: cd environments (tk runs jrsonnet)"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/cd-envs", "environments/loki-cd", "environments/mimir-cd", "environments/tempo-cd", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: grafana-plugins-cd"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/grafana-plugins-cd", "environments/grafana-plugins-cd", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: grafana-bench-cd"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/grafana-bench-cd", "environments/grafana-bench-cd", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: k8s-images-exporter"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/k8s-images-exporter", "environments/k8s-images-exporter", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: grafana-o11y (tk runs jrsonnet)"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/grafana-o11y", "environments/grafana-o11y", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: two non jrsonnet environments"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/two-non-jrsonnet-environments", "environments/grafana-com", "environments/grafana-datasources", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: all loki environments"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/loki-all", "environments/loki", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: all cortex environments"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/cortex-all", "environments/cortex", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: all tempo environments"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/tempo-all", "environments/tempo", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: ruler-exporter"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/ruler-exporter", "environments/ruler-exporter", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: litmuschaos"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/litmuschaos", "environments/litmuschaos", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 3
dir_compare = true

[[commands]]
name = "export: crossplane-compositions"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/crossplane-compositions", "environments/crossplane-compositions", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: aws-eks-essentials"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/aws-eks-essentials", "environments/aws-eks-essentials", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: aws-load-balancer-controller"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/aws-load-balancer-controller", "environments/aws-load-balancer-controller", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: o11y-ingest"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/o11y-ingest", "environments/o11y-ingest", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: clickhouse-operator"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/clickhouse-operator", "environments/clickhouse-operator", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: k6-cloud"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/k6-cloud", "environments/k6-cloud", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true


[[commands]]
name = "export: wiki"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/wiki", "environments/wiki", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true

[[commands]]
name = "export: betterops-platform-monitoring"
args = ["export", "/tmp/tk-compare-export-{{EXEC_NAME}}/betterops-platform-monitoring", "environments/betterops-platform-monitoring", "--recursive", "{{EXPORT_FORMAT}}", "--merge-strategy=fail-on-conflicts"]
runs = 1
dir_compare = true
